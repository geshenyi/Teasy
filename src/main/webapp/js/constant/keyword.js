/**
 * Created by ssge on 2015/11/24.
 */

define([], function () {
    var keyword = [
        {
            regex: /\btap\b/,
            token: 'keyword'
        },
        {
            regex: /\bclick\b/,
            token: 'keyword'
        },
        {
            regex: /\bopen\b/,
            token: 'keyword'
        },
        {
            regex: /\burl\b/,
            token: 'keyword'
        },
        {
            regex: /\bhover\b/,
            token: 'keyword'
        },
        {
            regex: /\bover\b/,
            token: 'keyword'
        },
        {
            regex: /\bby\b/,
            token: 'keyword'
        },
        {
            regex: /\bverify\b/,
            token: 'keyword'
        },
        {
            regex: /\bis\b/,
            token: 'keyword'
        },
        {
            regex: /\bpresent\b/,
            token: 'keyword'
        },
        {
            regex: /\btype\b/,
            token: 'keyword'
        },
        {
            regex: /\bwait\b/,
            token: 'keyword'
        },
        {
            regex: /\bseconds\b/,
            token: 'keyword'
        },
        {
            regex: /\bapply\b/,
            token: 'keyword'
        }

    ];
    return keyword;
});